#include <stdio.h>
#include "Logica.h"

int main() {
    ESTADO *e = inicializar_estado();
    prompt(e);


    /*FILE * file;
    file = fopen("C:\\Users\\franc\\CLionProjects\\ProjetoLI2\\Tabuleiro.txt", "r+");
    le_Tabuleiro(e,file);
    mostrar_tabuleiro(e);*/

    return 0;
}